# Test Environment Variables Configuration
# This file contains all environment variables used during testing
# Copy and modify these values as needed for your test environment

# External API Configuration (Mock URLs for testing)
GEOAPIFY_API_KEY=test-geoapify-api-key-12345
GEOAPIFY_BASE_URL=http://localhost:8089/mock-geoapify
OPENMETEO_BASE_URL=http://localhost:8090/mock-openmeteo

# JWT Configuration for Security Tests
JWT_JWK_SET_URI=http://localhost:8180/realms/test/protocol/openid-connect/certs

# Kafka Configuration (Use embedded Kafka for tests)
KAFKA_BOOTSTRAP_SERVERS=embedded
KAFKA_BROKERS=embedded
SPRING_KAFKA_BOOTSTRAP_SERVERS=embedded
SPRING_CLOUD_STREAM_KAFKA_BINDER_BROKERS=embedded

# Database Configuration (H2 in-memory for tests)
SPRING_DATASOURCE_URL=jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE;DB_CLOSE_DELAY=-1
SPRING_DATASOURCE_USERNAME=sa
SPRING_DATASOURCE_PASSWORD=

# Spring Profiles
SPRING_PROFILES_ACTIVE=test

# Disable external services for testing
EUREKA_CLIENT_ENABLED=false
SPRING_CLOUD_STREAM_ENABLED=false